---
all:
  children:
    kafka_server:
      hosts:
        kafka-1.example.invalid:
        kafka-2.example.invalid:
        kafka-3.example.invalid:

    k8s:
      children:
        k8s_control_plane:
          hosts:
            control-plane-1.example.invalid:

        k8s_worker:
          hosts:
            worker-1.example.invalid:
            worker-2.example.invalid:

    # Workers that provide local disks for Longhorn. The control plane stays out of
    # Longhorn data placement so it remains dedicated to Kubernetes control-plane work.
    longhorn_storage:
      hosts:
        worker-1.example.invalid:
        worker-2.example.invalid:
